import random
import os
import sys
from datetime import datetime

from django.db import connection
from django.contrib.auth.models import User


def run():

    print('Running')

    # set_range = "hours"
    # set_length = 1

    # account = "time.in.progress"

    # youtube.get_difference(account, set_range, set_length)
